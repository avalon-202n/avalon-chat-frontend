import { StyleSheet } from "react-native";
import { windowWidth } from "@enum/size";
const styles = StyleSheet.create({
  headerRightImg: {
    width: windowWidth * 30,
    height: windowWidth * 30,
  },
});

export default styles;
